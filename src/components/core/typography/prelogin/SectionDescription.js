import React from 'react'

const SectionDescription = ({children}) => {
    return (
        <p className="preLoginContentSectionDescriptionTxt">{children}</p>
    )
}

export default SectionDescription