import React from 'react';
import { icons } from '../../../../../../constants';

const HeaderRightMenu = ({ children }) => {
    return (
        <div className="dashboardHeaderRightMenu">
            {children}
        </div>
    )
}

export default HeaderRightMenu