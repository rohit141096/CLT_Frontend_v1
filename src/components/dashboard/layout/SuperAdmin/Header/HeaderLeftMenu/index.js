import React from 'react';
import { icons } from '../../../../../../constants';

const HeaderLeftMenu = ({ children }) => {
    return (
        <div className="dashboardHeaderLeftMenu">
            {children}
        </div>
    )
}

export default HeaderLeftMenu